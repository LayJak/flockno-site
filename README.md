# FlockNo public site

Upload every file in this folder directly to the root of the public `flockno-site` repository.

This version uses:
- `icon-source.png` — transparent FlockNo icon used in the dark header/favicon
- `product-camera-details.png` — product screenshot used in the hero and showcase
- `product-privacy-report.png` — product screenshot used in the showcase
- HTML/CSS brand text instead of the white-background horizontal lockup

The site contains no extension source, backend source, API credentials, private email address, or development secrets.
